# Agent 评测

用于统一 Agent 测试与评价活动中的业务术语。

## Language

**评价口径**：
判定 Agent 响应所采用的评价模型、指标、阈值和评分规则等条件。恢复评价时，既有与当前评价口径及其他约定变量的一致性由用户保证，发起恢复即视为用户确认一致。

**恢复评价**：
基于已保存的 Agent 响应继续或重新进行的评价活动，沿用既有评价口径，不包含重新请求 Agent。
_Avoid_: Agent 重跑

**全量重新评价**：
对当前选定范围内所有可评价用例重新进行评价的恢复方式，不读取或复用历史评价结论，仅使用已保存的 Agent 响应。

**增量恢复评价**：
依据指定历史结果中的完整评价批次复用已通过结论，仅对当前选定范围内未通过或没有可信评价结果的可评价用例重新进行评价的恢复方式。

**完整评价批次**：
一组已经保存且完整性可验证的评价结果，是异常中断后可采纳的恢复依据；未确认完整的批次不属于可复用结果，需要重新评价。批次完整仅表示结果已完整保存，不表示其中所有用例评价通过。

**不可评价用例**：
因所需 Agent 响应缺失而无法开展评价的用例，多轮用例任一轮响应缺失即属于此类。其整体结论为未通过且保留失败记录，但不调用该用例的评价模型，也不将未经评价的其他轮次标记为评价失败。

**轮次评价结论**：
多轮用例中某一轮对话自身的评价判定，不因其他轮次未通过而改变。
_Avoid_: 用例整体结论

**多轮用例整体结论**：
同一用例所有轮次评价结论的合并判定，仅全部轮次通过时才通过，不改写各轮结论或错误信息。
_Avoid_: 第一轮结论
