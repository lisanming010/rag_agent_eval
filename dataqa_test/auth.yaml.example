# 复制为 dataqa_test/auth.yaml 后填写；auth.yaml 已被忽略，不会提交。
login_url: https://intelligent-bi.codeartz.xyz/api/system/login
username: Leo01
password: "replace-with-real-password"
tenant_id: "146085512914162117"
accept_language: en-US
login_timeout: 30
request_timeout: 120

# 可选；未配置时复用项目 config.yaml 中的 DataQA endpoint。
agent_endpoint: ""

